import mutations from 'src/store'
import { ADD_PRODUCT, INCREMENT_PRODUCT, DECREMENT_PRODUCT, REMOVE_PRODUCT } from 'src/types'
describe('mutations.js',() =>{   
    let state  
    beforeEach(()=>{
        state = {
            basket: []
        }
    })
})